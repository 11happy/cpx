name: Rust CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose

      - name: Lint with Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Check formatting
        run: cargo fmt -- --check

      # 32-bit glibc has a 32-bit off_t; catch type mismatches before release.
      - name: Type-check the armv7 glibc target
        if: runner.os == 'Linux'
        run: |
          rustup target add armv7-unknown-linux-gnueabihf
          cargo check --target armv7-unknown-linux-gnueabihf

      - name: Build release binary for the GNU suite
        if: runner.os == 'Linux'
        run: cargo build --release

      - name: Run ported GNU coreutils cp tests
        if: runner.os == 'Linux'
        run: PATH="$PWD/target/release:$PATH" tests/gnu/run.sh
